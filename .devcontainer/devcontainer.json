{
  "name": "AzadAccounting-sys",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",
  "forwardPorts": [5000],
  "portsAttributes": {
    "5000": {
      "label": "AzadAccounting-sys",
      "onAutoForward": "notify"
    }
  },
  "postCreateCommand": "python -m venv .venv && . .venv/bin/activate && pip install --upgrade pip wheel setuptools && pip install -r requirements.txt",
  "postAttachCommand": "echo 'Run: source .venv/bin/activate && export APP_ENV=local && export FLASK_APP=app:create_app && flask db upgrade && python app.py'"
}
